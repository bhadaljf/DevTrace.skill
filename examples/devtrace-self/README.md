# DevTrace Self Example

这是一个用 **DevTrace 自己** 作为样本项目生成的最小示例。

它展示了：

- `00_INDEX.md` 如何作为生成内容索引工作
- `01_CURRENT.md` 如何作为当前状态入口工作
- `trace/*.yaml` 如何保存跨时间事件的 `TraceUnit`
- `reviews/` 如何承载会话总结与阶段回顾

这个示例不追求覆盖所有历史，只覆盖当前仓库最近两天最关键的演化过程，目的是验证 Skill 版 DevTrace 的当前结构是否可用。
